# 현재 사용 버전 (app/) - 최소 필수 의존성만 포함
# FastAPI 웹 프레임워크
fastapi==0.110.0
uvicorn[standard]==0.29.0

# 설정 및 환경변수
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# OpenAI API (일정 생성용)
openai==1.3.8

# HTTP 클라이언트 (관광공사 API 호출용)
httpx==0.25.0

# 파일 업로드 처리
python-multipart==0.0.6