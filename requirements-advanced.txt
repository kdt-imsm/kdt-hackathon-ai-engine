# 고도화 버전 (advanced_features/) - 전체 의존성 포함
# 현재 사용 버전의 모든 의존성 + 추가 고급 기능

# ---- 기본 FastAPI 웹 프레임워크 ----
fastapi==0.110.0
uvicorn[standard]==0.29.0

# ---- 설정 및 환경변수 ----
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# ---- AI 및 자연어 처리 ----
openai==1.3.8

# ---- HTTP 클라이언트 ----
httpx==0.25.0

# ---- 파일 처리 ----
python-multipart==0.0.6

# ---- 데이터베이스 (PostgreSQL + pgvector) ----
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
pgvector==0.2.4
alembic==1.13.1

# ---- 데이터 처리 및 분석 ----
pandas==2.1.4
numpy==1.24.3

# ---- 벡터 검색 및 임베딩 ----
scikit-learn==1.3.2
sentence-transformers==2.2.2

# ---- 캐싱 ----
redis==5.0.1

# ---- 테스팅 ----
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.0  # 테스트용 HTTP 클라이언트

# ---- 개발 도구 ----
black==23.11.0
flake8==6.1.0
mypy==1.7.1

# ---- 추가 유틸리티 ----
click==8.1.7      # CLI 도구
tqdm==4.66.1      # 프로그레스 바
python-dateutil==2.8.2  # 날짜 처리